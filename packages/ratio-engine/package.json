{
  "name": "@kitchenkit/ratio-engine",
  "version": "0.1.0",
  "description": "Pure TypeScript recipe ratio math engine — zero deps. Shared with CulinaryOS.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "type": "module",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
