{
  "name": "@kitchenkit/prep-mcp",
  "version": "0.1.0",
  "description": "MCP server — shift prep tools for CulinaryOS AI bridge.",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node --esm src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kitchenkit/prep-engine": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0"
  }
}
